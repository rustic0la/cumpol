query getDomains {
  getDomains {
    ...getDomainsDomain
  }
}

fragment getDomainsDomain on Domain {
  id
  title
}

query getCollections($domainId: ID!) {
  getCollections(domainId: $domainId) {
    ...collection
  }
}
