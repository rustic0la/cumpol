mutation signup($username: String!, $password: String!) {
  signup(username: $username, password: $password) {
    token
    error
  }
}

mutation login($username: String!, $password: String!) {
  login(username: $username, password: $password) {
    token
    error
    user {
      id
    }
  }
}

mutation addSpace($title: String!) {
  addSpace(title: $title) {
    id
    title
  }
}

mutation updateSpace($spaceId: String!, $title: String!) {
  updateSpace(spaceId: $spaceId, title: $title) {
    id
    title
  }
}

mutation deleteSpace($spaceId: String!) {
  deleteSpace(spaceId: $spaceId)
}

mutation deleteTopic($topicId: String!) {
  deleteTopic(topicId: $topicId)
}

mutation updateTopic($topicId: String!, $title: String!) {
  updateTopic(topicId: $topicId, title: $title) {
    ...topic
  }
}

mutation addTopic($title: String!, $spaceId: String!) {
  addTopic(spaceId: $spaceId, title: $title) {
    ...topic
  }
}

mutation addTodoList($title: String!, $topicId: String!) {
  addTodoList(title: $title, topicId: $topicId) {
    ...todoList
  }
}

mutation updateTodoList($title: String!, $todoListId: String!) {
  updateTodoList(title: $title, todoListId: $todoListId) {
    ...todoList
  }
}

mutation deleteTodoList($todoListId: String!) {
  deleteTodoList(todoListId: $todoListId)
}

mutation addTodo($title: String!, $todoListId: String!) {
  addTodo(title: $title, todoListId: $todoListId) {
    ...todo
  }
}

mutation updateTodo($title: String!, $todoId: String!) {
  updateTodo(title: $title, todoId: $todoId) {
    ...todo
  }
}

mutation deleteTodo($todoId: String!) {
  deleteTodo(todoId: $todoId)
}
