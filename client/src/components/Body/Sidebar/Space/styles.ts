import styled from 'styled-components';

export const SpaceStyled = styled.input`
  padding: 10px 20px;
  margin: 10px 0;
  background: none;
  border: none;

  :active,
  :focused,
  :visited {
    border: none;
  }
`;
